# תוכנית בנייה

> **המטרה: משהו אמיתי באוויר היום.** מחולק כך שכל שלב עומד בפני עצמו — אפשר לעצור בסוף כל אחד ועדיין להיות במצב טוב.

---

## שלב 0 — הכנה (15 דק׳)

- [ ] המשתמש יוצר פרויקט ב-[supabase.com](https://supabase.com) (חינם)
- [ ] מריץ את `db/schema.sql` ב-SQL Editor
- [ ] מפעיל Email / Magic Link ב-Authentication → Providers
- [ ] מעתיק Project URL + anon key ל-`public/env.js`
- [ ] יוצר חשבון Netlify

**⚠️ בלי הפרטים האלה אי אפשר להתקדם. אם חסרים — עצור ובקש.**

---

## שלב 1 — קריאה בלבד ⚡ **זה מה שעולה לאוויר היום**

מטרה: אתר חי שמושך צרכים אמיתיים מ-Supabase ומציג אותם.

- [ ] `index.html` — מבוסס על `design/mockup.html`, **אותו עיצוב בדיוק**
- [ ] טעינת `supabase-js` מ-CDN
- [ ] פיד צרכים — נשלף מהשרת במקום מהמערך הקבוע
- [ ] סינון לפי סוג (חפץ/שינוע/זמן) ולפי אזור
- [ ] פיד הצלחות — מ-`success_feed`
- [ ] מסך חסויות — נטען מ-`public/config.json`
- [ ] קרדיט "פותח ונבנה על ידי" + כפתור וואטסאפ
- [ ] מצבי טעינה, ריק, ושגיאה — **חובה.** אתר שנתקע על מסך לבן זה כישלון.
- [ ] הזנת 3–5 חוות לדוגמה ידנית דרך ה-Dashboard

✅ **סוף שלב 1 = מעלים ל-Netlify ושולחים לאנשים.**

---

## שלב 2 — הצעת עזרה (ללא הרשמה)

- [ ] כפתור "אני רוצה לעזור" → טופס קצר (שם + טלפון + הערה)
- [ ] שמירה ב-`offers`
- [ ] חשיפת טלפון החווה **רק אחרי** שליחת ההצעה
- [ ] הסטטוס עובר אוטומטית ל-`committed` (טריגר בבסיס הנתונים)
- [ ] הגנה בסיסית מספאם — השבתת הכפתור בזמן שליחה, בדיקת תקינות טלפון

✅ **סוף שלב 2 = הלולאה עובדת מקצה לקצה בלי הרשמה.**

---

## שלב 3 — צד המתיישב (הרשמה)

- [ ] התחברות ב-Magic Link
- [ ] "צור חווה" — נוצרת `verified=false`
- [ ] מסך "הצרכים שלי"
- [ ] פרסום צורך חדש
- [ ] סימון "נמסר" → עולה לפיד ההצלחות
- [ ] צפייה בהצעות שהתקבלו (שם + טלפון)
- [ ] מסך המתנה ברור לחווה שטרם אושרה

✅ **סוף שלב 3 = מתיישבים מנהלים את עצמם.**

---

## שלב 4 — אדמין

- [ ] `admin.html` — נגיש רק למי שב-`admins`
- [ ] רשימת חוות ממתינות + כפתור אישור
- [ ] תצוגת כלל הצרכים והסטטוסים
- [ ] אפשרות למחוק צורך פוגעני

---

## שלב 5 — ליטוש (לא חוסם השקה)

- [ ] PWA — אייקון למסך הבית (`manifest.json`)
- [ ] תגי OG לשיתוף יפה בוואטסאפ
- [ ] פאביקון מ-`design/hazani-icon.png`
- [ ] נגישות: ניגודיות, גדלי מגע, `alt`
- [ ] Google Analytics או Plausible

---

## הערכת זמן מציאותית

| שלב | זמן |
|---|---|
| 0 + 1 | **2–4 שעות** ← ריאלי להיום |
| 2 | 1–2 שעות |
| 3 | 3–5 שעות |
| 4 | 1–2 שעות |
| 5 | לפי הצורך |

**מה באמת יכול לעלות היום:** שלבים 0–1, ואם הכל זורם גם 2. שלב 3 (הרשמה) ריאלי מחר.

**הצעה:** אל תחכה לשלב 3 כדי לשלוח לאנשים. אתר קריאה-בלבד עם 5 חוות אמיתיות כבר נותן משוב אמיתי — ובינתיים אתה מזין צרכים ידנית דרך ה-Dashboard.

---

## סדר עבודה לכל שלב

1. בנה
2. הרץ מקומית ובדוק בעצמך
3. **בדוק במיוחד: מה קורה בלי רשת? עם נתונים ריקים? בשגיאה?**
4. העלה ל-Netlify
5. בדוק בנייד אמיתי — לא רק בדסקטופ

---

## שלב 3.5 — צד ההיצע: "יש לי מה לתת" 🎁 (אושר 2026-07-21: 1א 2א 3א)

- [ ] `db/donations.sql` — טבלה + views + שלוש פונקציות (פרסום/שיריון/נמסר)
- [ ] מתג בראש פיד הצרכים: "מבקשים 🌾 | נותנים 🎁" (בלי טאב חמישי)
- [ ] טופס "יש לי מה לתרום" — בלי הרשמה: סוג, כותרת, פירוט, אזור, שם, טלפון
- [ ] תרומה נכנסת `approved=false` — **אישור ידני של אדמין** לפני שמופיעה
- [ ] טלפון הנותן נחשף **רק** לבעל חווה מאומתת שלחץ "מתאים לחווה שלי"
- [ ] "נמסר" ע"י החווה המשויכת → מצטרף לפיד ההצלחות ולמונה
- [ ] גרסה 1.1.0
