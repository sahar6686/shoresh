# שורש 🌱 — הוראות בנייה

> קרא את הקובץ הזה קודם. הוא כל מה שצריך כדי להתחיל לבנות.

## מה בונים

פלטפורמת סולידריות שמחברת מתיישבי חוות בגולן, בגליל, בנגב ובהר עם אנשים שרוצים לתת יד.

**הלולאה:** מתיישב מפרסם צורך → נותן מתחבר → קשר ישיר → נמסר ✓ → עולה לפיד ההצלחות.

**שלוש דרכי נתינה:** 📦 חפץ · 🚚 שינוע · 🖐️ זמן

## סטאק — לא לשנות בלי לשאול

| רכיב | בחירה | למה |
|---|---|---|
| Frontend | **HTML + CSS + Vanilla JS** | בלי שלב build. המוקאפ כבר בנוי כך — ממשיכים ממנו. |
| Backend | **Supabase** (Postgres + Auth) | שכבה חינמית, עובד ישירות מאתר סטטי, אין שרת לתחזק. |
| חיבור | `@supabase/supabase-js` דרך **CDN** | `<script>` אחד. בלי npm, בלי bundler. |
| אירוח | **Netlify** (drag & drop) | העלאה תוך שניות. |
| RTL | עברית, `dir="rtl"` | קהל היעד. |

**אין React. אין TypeScript. אין Tailwind. אין שלב build.** המטרה היא לעלות לאוויר היום.

## סדר קריאה

1. `docs/BUILD-PLAN.md` — **התוכנית. התחל כאן.** מחולק לשלבים; שלב 1 עולה לאוויר היום.
2. `docs/DATA-MODEL.md` — הישויות והשדות
3. `db/schema.sql` — סכמת Supabase מוכנה להרצה
4. `docs/DECISIONS.md` — החלטות מוצר שכבר נסגרו. **אל תפתח מחדש.**
5. `docs/PARKED.md` — רעיונות שנפסלו. **אל תציע אותם.**
6. `design/mockup.html` — **המוקאפ החזותי. זה הרפרנס לעיצוב.** פתח אותו בדפדפן.
7. `docs/ACCEPTANCE.md` — איך יודעים שסיימנו

## חוקי ברזל

1. **העיצוב מגיע מהמוקאפ.** `design/mockup.html` הוא מקור האמת לצבעים, מרווחים, פונטים ומבנה. אל תמציא עיצוב חדש — העתק משם.
2. **פשטות ברמת סבתא.** מתיישבים מפרסמים בעצמם, וחלקם פחות טכנולוגיים. כפתורים גדולים, מעט שדות, אפס ז'רגון.
3. **בלי פרסומות. בלי גביית כסף מהמשתמשים.** אין סכומי תרומה, אין סליקה. ההכנסה מחסויות בלבד.
4. **חסויות נטענות מ-`public/config.json`** — לא צרובות בקוד. חייב מתג `active` והתנהגות תקינה כשאין רשת.
5. **אל תוסיף פיצ'רים שלא בתוכנית.** אם עולה רעיון — כתוב אותו ב-`docs/IDEAS.md` והמשך.
6. **אין סודות בקוד.** מפתח Supabase `anon` הוא ציבורי ובסדר. **`service_role` לעולם לא בצד לקוח.**

## סביבה

צור `public/env.js` (לא נכנס ל-git):

```js
window.SUPABASE_URL = "https://xxxxx.supabase.co";
window.SUPABASE_ANON_KEY = "eyJhbGci...";
```

המשתמש ייצור פרויקט Supabase ויספק את שני הערכים. **אם הם חסרים — עצור ובקש אותם.**

## מצב נוכחי

- ✅ מוקאפ עובד (in-memory, בלי שרת)
- ✅ אפיון, החלטות, תקציב
- ✅ לוגו של משק חזני לקרדיט "פותח ונבנה על ידי"
- ❌ אין בסיס נתונים, אין הרשמה, אין סנכרון בין משתמשים

**הפער שסוגרים עכשיו:** להפוך את המוקאפ למוצר עם נתונים משותפים אמיתיים.

## מי המשתמש

סהראל — רקע בהנדסה, תפעול וניהול מפעל. מבין טכנית. מעדיף תשובות ישירות וצפופות.
**עונה על שאלות עד הבנה של 95%+ לפני ביצוע — אם משהו לא ברור, שאל לפני שאתה בונה.**
